import { Theme } from "@mui/system";
import { SxProps } from "@mui/material";

export const EVENT_CALENDAR = {
  "& .event-closed-slots": {
    color: "#fff",
    background: "#f44336",
    opacity: 0.8,
    fontWeight: "bold",
    fontSize: "0.8em",
    textAlign: "center",
    borderRadius: 4,
    padding: 2,
  },
};
